services:
  - type: web
    name: diabetes-detection-api
    env: python
    region: singapore
    plan: free
    rootDir: backend
    buildCommand: pip install -r ../requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    pythonVersion: 3.10.13
